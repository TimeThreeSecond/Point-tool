# 切点 (SwitchPoint) 产品需求文档 (PRD)

> **版本**: v1.1
> **日期**: 2026-06-09
> **状态**: 自用开发中
> **目标平台**: Windows 桌面端 (Phase 1) → Android (Phase 2)

---

## 1. 项目概述

### 1.1 产品定位
**切点 (SwitchPoint)** 是一个给自己用的个人工具，核心目标只有一个——在自己刷短视频/碎片娱乐停不下来的时候，用一次 **5 秒的强制中断**把人拔出来。

不是效率工具，不是习惯养成，不是游戏化平台。就是一把给自己准备的"钩子"。

### 1.2 核心原则
- **自己用**：不做商业化、不上架、不需要获客
- **不要完美**：没有截止日期，MVP 跑起来看看效果再说
- **君子协议**：不做强制措施，任务完成了没有系统验证，自己诚实地选"完成"

---

## 2. 目标用户

就我自己。使用场景：
- 本想刷 10 分钟短视频结果刷了 2 小时
- 休息间隙拿起手机就放不下
- 知道自己该停但缺一个"外部信号"

---

## 3. 核心功能

### 3.1 触发机制：娱乐前台占比检测

**核心逻辑**：不设固定计时器，而是检测**一段时间内娱乐软件占据前台的时间比例**。

**工作方式**：
```
用户配置娱乐软件名单（浏览器、抖音、游戏等）
             ↓
系统持续轮询当前前台窗口
             ↓
在滑动时间窗口内统计：娱乐前台时间 / 总时间
             ↓
当娱乐占比超过阈值（如 70%）→ 触发打断弹窗
             ↓
弹窗关闭后立即重置计数窗口
```

**关键参数**（默认值，用户可调）：
| 参数 | 默认值 | 说明 |
|------|--------|------|
| 检测时间窗口 | 30 分钟 | 滑动窗口大小 |
| 触发阈值 | 70% | 娱乐前台占比超过此值触发弹窗 |
| 强制等待 | 8 秒 | 弹窗出现后，按钮禁用时间 |
| 累计娱乐下限 | 3 分钟 | 窗口内娱乐时间低于此值不触发（防误触） |

**为什么这样设计**：
- 在做事（前台是工作软件）→ 永远不会被打断 → 保护心流
- 在刷手机/视频（前台是浏览器/娱乐软件）→ 持续一定时间后打断 → 给人一个"停"的信号
- 不判断用户意图，只看事实前台占比

### 3.2 打断弹窗

#### 3.2.1 窗口属性
- **位置**: 屏幕右上角固定位置，主显示器
- **尺寸**: 约 360×480 像素（DPI 自适应）
- **层级**: 系统置顶 (Always on Top)，覆盖所有其他窗口
- **行为限制**: 不可最小化、不可 Alt+F4、仅通过应用内按钮关闭

#### 3.2.2 交互流程

**5 秒强制等待期**：
- 显示倒计时（大数字或进度条）
- 骰子（仅作为装饰/互动元素，随便点着玩）
- 按钮禁用

**5 秒后**：
- 「继续工作」按钮可点击 → 关闭弹窗，回归正常
- 「掷骰子」按钮可点击 → 纯粹娱乐，出了什么不影响任何东西
- 「君子任务」按钮可点击 → 弹出一条任务（俯卧撑/喝水等），自己选做完或没做完

### 3.3 骰子系统

**定位**：纯装饰/互动玩具，不是核心玩法。

简化方案：
- 只保留一种骰子（六面）
- 点击随机转一个数字出来，仅作为视觉反馈
- 不累计积分、不关联任何奖励/惩罚
- 不喜欢可以关掉，只保留倒计时

### 3.4 君子任务系统

**定位**：给你一个"想起来了就顺便做一下"的提示。

- 弹窗显示一条任务（来自预设任务池）
- 两个按钮：「已完成」「没完成」
- **不做系统验证**，自己诚实选
- 任务完成有简单的"连击天数"统计（纯给自己看）
- 不完成没有任何惩罚

### 3.5 统计数据

- 当日打断次数
- 娱乐→打断的时间分布（看看自己什么时候最容易沉迷）
- 任务完成率（自己报的）
- **不做**积分系统、不做排行榜、不做成就

---

## 4. 用户旅程

```
[用户启动 SwitchPoint，后台运行]
            ↓
[配置娱乐软件名单 + 参数（一次性的）]
            ↓
[系统后台轮询前台窗口]
            ↓
    （如果在工作软件上，一切静默）
            ↓
[检测到娱乐占比超过阈值]
            ↓
[弹出置顶打断窗口，右上角固定位置]
            ↓
        5 秒倒计时
            ↓
[继续工作] ← 或 → [君子任务]
  关闭弹窗        弹出任务提示
                    ↓
               [已完成] / [没完成]
                    ↓
              关闭弹窗，继续检测
```

---

## 5. UI 需求

### 5.1 设计风格
- 轻量、不严肃、不给人压力
- 主色调: `#6366F1`（靛紫）
- 背景跟随系统明暗
- 圆角 16px / 8px

### 5.2 打断弹窗
- 标题: "休息一下" 或类似轻松语气
- 大倒计时数字
- 骰子（可选显示）
- 两个/三个按钮
- 没有警告、没有指责语气

### 5.3 设置页
- 娱乐软件名单编辑（增删）
- 时间窗口长度
- 触发阈值
- 强制等待时长
- 开机自启选项

---

## 6. 技术架构

### 6.1 技术栈
| 层级 | 技术 | 说明 |
|------|------|------|
| 框架 | Flutter | 跨平台，Windows + Android |
| 语言 | Dart + C++ (Win32 FFI) | 前台窗口检测需调用 Win32 API |
| 状态管理 | ChangeNotifier | MVP 够用，后续按需升级 |
| 本地存储 | Hive | 应用配置 + 统计持久化 |
| 窗口管理 | window_manager | 置顶弹窗 |
| 系统托盘 | system_tray | 后台运行入口 |

### 6.2 架构分层

```
┌──────────────────────────────────┐
│  UI 层                           │
│  - 打断弹窗 / 设置页              │
│  - 状态: ChangeNotifier           │
├──────────────────────────────────┤
│  检测层                           │
│  ┌────────────────────────────┐  │
│  │ ForegroundDetector         │  │ ← 抽象接口
│  │  ├─ Win32Detector (FFI)    │  │    Windows: GetForegroundWindow
│  │  └─ AndroidDetector(待定)   │  │    Android: UsageStatsManager
│  └────────────────────────────┘  │
│  ┌────────────────────────────┐  │
│  │ TimeRatioTracker           │  │ ← 滑动窗口占比计算
│  │ TriggerScheduler           │  │ ← 触发决策
│  └────────────────────────────┘  │
├──────────────────────────────────┤
│  服务层                           │
│  - TaskService                  │
│  - ConfigStore (Hive)           │
└──────────────────────────────────┘
```

### 6.3 跨平台策略
- 核心业务逻辑（比例计算、触发决策、任务管理）跨平台共享
- 平台检测层通过抽象接口隔离
- Phase 1：实现 Win32 FFI 检测
- Phase 2：实现 Android UsageStatsManager 检测

---

## 7. 数据模型

### Settings（设置）
```json
{
  "entertainment_apps": ["chrome", "msedge", "抖音"],
  "detection_window_minutes": 30,
  "trigger_threshold": 0.7,
  "force_wait_seconds": 8,
  "min_entertainment_minutes": 3,
  "auto_start": false
}
```

### Task（君子任务）
```json
{
  "id": "uuid",
  "content": "做 5 个俯卧撑",
  "category": "body",
  "completed_today": false,
  "completion_dates": ["2026-06-09"]
}
```

### Stats（统计，纯自己看）
```json
{
  "daily_breaks": 5,
  "tasks_completed": 3,
  "current_streak_days": 2,
  "hourly_triggers": {"14": 2, "15": 3}
}
```

---

## 8. 开发规划

### Phase 1: Windows MVP
1. 娱乐前台占比检测引擎（Win32 FFI）
2. 可配置的娱乐软件名单
3. 置顶打断弹窗
4. 君子任务系统
5. Hive 持久化
6. 系统托盘后台运行

### Phase 2: Android
1. Android 前台检测（UsageStatsManager）
2. 适配弹窗 UI
3. 配置同步（可选）

---

## 9. 术语表

| 术语 | 定义 |
|------|------|
| 娱乐前台占比 | 在滑动时间窗口内，娱乐软件占据前台的时间比例 |
| 检测窗口 | 用于计算占比的时间窗口长度 |
| 强制等待 | 弹窗出现后按钮禁用的时间 |
| 君子任务 | 自己申报完成/未完成的任务，无系统验证 |
| SwitchPoint | "切点"的英文名，打断时刻 |

---

*v1.1 更新：核心触发机制改为娱乐占比检测，骰子降级为纯装饰，新增君子协议任务系统，移除商业化相关规划。*
