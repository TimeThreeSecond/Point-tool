# 切点 (SwitchPoint) 产品需求文档 (PRD)

> **版本**: v1.0  
> **日期**: 2026-04-25  
> **状态**: MVP 开发就绪  
> **目标平台**: Windows 桌面端 (Phase 1) → Android (Phase 2)

---

## 1. 项目概述

### 1.1 产品定位
**切点 (SwitchPoint)** 是一款面向个人的「休闲效率工具」，核心目标是通过**低侵入性的娱乐化打断机制**，帮助用户从短视频等沉浸式娱乐场景中自然脱离，并为工作/学习状态提供软着陆入口。

### 1.2 核心矛盾与解法
| 矛盾 | 解法 |
|------|------|
| 用户想放松，但一刷短视频就停不下来 | 不禁止娱乐，而是周期性插入**娱乐化打断点** |
| 传统效率工具太生硬（强制锁屏/弹窗警告） | 用**随机赌博小游戏（掷骰子）**降低被打断的抵触感 |
| 心流状态需要保护 | **5-10秒强制等待**后即可无负担关闭，不摧毁深度工作状态 |
| 从娱乐到工作的切换太突兀 | 通过**随机任务**作为缓冲带，给用户思考"接下来做什么"的时间 |

### 1.3 产品口号
> "在娱乐与专注之间，给自己一个切点。"

---

## 2. 目标用户

- **主要用户**: 大学生、年轻职场人，有自我提升意愿但自制力有限
- **典型场景**: 
  - 本想刷10分钟短视频放松，结果刷了2小时
  - 学习/工作间隙想休息，但无法重新进入状态
  - 知道自己该做什么，但缺乏一个"启动信号"

---

## 3. 核心功能需求

### 3.1 模式管理

#### 3.1.1 手动开启/关闭
- 应用以**系统托盘**形式后台运行，默认不显示窗口
- 用户通过托盘菜单主动开启「切点模式」
- 开启后进入**工作-打断-休息**循环，直至用户手动关闭

**全局快捷键**：
| 快捷键 | 功能 | 说明 |
|--------|------|------|
| `Ctrl + Shift + S` | 开启/关闭切点模式 | 无需打开托盘菜单，快速切换 |
| `Ctrl + Shift + P` | 紧急暂停 | 临时暂停计时器，适用于会议等紧急场景 |
| `Ctrl + Shift + O` | 打开设置窗口 | 快速呼出配置界面 |

#### 3.1.3 勿扰模式
- 用户可设置「勿扰时段」（如 22:00 - 08:00）
- 勿扰期间：计时器暂停，不触发弹窗
- 或设置「单次勿扰」：暂停 30/60/120 分钟后自动恢复
- 勿扰状态在托盘图标上显示特殊标记（如月亮图标）

#### 3.1.2 自定义周期
| 参数 | 默认值 | 可调范围 | 说明 |
|------|--------|----------|------|
| 工作时长 (T1) | 30 分钟 | 10-120 分钟 | 两次打断之间的间隔 |
| 休息时长 (T2) | 5 分钟 | 1-60 分钟 | 接受惩罚任务后的自由时间 |
| 强制等待时长 | 8 秒 | 5-15 秒 | 弹窗出现后"继续工作"按钮的禁用时间 |
| 默认骰子 | 六面骰 (d6) | d2/d3/d6/d20 | 每次打断前可临时切换 |

### 3.2 打断提醒窗口

#### 3.2.1 窗口属性
- **位置**: 屏幕右上角固定位置，**主显示器**
  - 通过 `GetPrimaryMonitor` API 获取系统主显示器，确保弹窗始终在分辨率/缩放比例稳定的屏幕上显示
  - 多显示器环境下，不跟随鼠标移动，避免用户将娱乐窗口拖到副屏后弹窗错位
- **尺寸**: 约 360×480 像素（主显示器 DPI 自适应，在高分屏上自动缩放）
- **层级**: 系统置顶 (Always on Top)，覆盖所有其他窗口
- **行为限制**:
  - 不可最小化
  - 不可通过 Alt+F4 关闭
  - 不可通过任务栏关闭
  - 仅可通过应用内按钮或托盘菜单关闭

#### 3.2.2 两阶段交互设计

**阶段 A：强制吸引期（5-10 秒）**
- 窗口中央展示**3D 旋转骰子动画**
  - 使用 `flutter_cube` 或自定义 OpenGL 渲染 3D 骰子模型
  - 支持四档骰子（d2/d3/d6/d20）的 3D 模型切换
  - 动画为缓慢自转 + 悬浮效果，帧率 60fps
  - 点击骰子可触发一次快速翻滚动画（娱乐性交互）
- 下方显示倒计时进度条
- **「继续工作」按钮呈灰色禁用状态**
- **「掷骰子」按钮始终可用**（吸引娱乐状态用户）
- 心流用户：瞟一眼 → 等待倒计时 → 点击关闭 → 零负担回归工作
- 娱乐用户：被动画吸引注意力 → 自然脱离短视频沉浸

**阶段 B：自由决策期（倒计时结束后）**
- 「继续工作」按钮激活，点击后窗口关闭，进入下一个 T1 周期
- 「掷骰子」按钮仍可点击，进入赌博流程

### 3.3 骰子与赌博系统

#### 3.3.1 骰子类型
| 名称 | 面数 | 奖励概率 | 奖励积分 | 惩罚概率 | 定位 |
|------|------|----------|----------|----------|------|
| 硬币 (Coin) | 2 | 50% | 1 分 | 50% | 新手默认，低风险低收益 |
| 三角骰 (d3) | 3 | 33.3% | 3 分 | 66.7% | 进阶 |
| 六面骰 (d6) | 6 | 16.7% | 6 分 | 83.3% | 平衡选择 |
| 二十面骰 (d20) | 20 | 5% | 20 分 | 95% | 高风险高收益，赌狗专用 |

**规则**:
- 奖励判定：掷出最大点数（如 d6 掷出 6）
- 奖励积分 = 骰子面数
- 用户每次打断前可自由切换骰子类型
- 纯运气机制，无动态概率调整

#### 3.3.2 奖励路径
- 显示「🎉 奖励！+N 分」
- 展示当前总积分
- 用户点击「收下并继续」后窗口关闭，进入下一个 T1

#### 3.3.3 惩罚路径
- 显示「⚡ 命运选择了你」
- 从任务池中**随机抽取一个任务**
- 用户点击「开始任务 (N分钟)」后：
  - 窗口关闭
  - 进入 T2 休息/任务时间
  - T2 结束后自动进入下一个 T1
- 用户点击「换一件」可重新抽取一次任务

### 3.4 任务系统

#### 3.4.1 任务来源
| 来源 | 优先级 | 说明 |
|------|--------|------|
| 用户预设任务 | 70% 抽取权重 | 用户在设置页自行添加的个人目标 |
| 系统预设任务 | 30% 抽取权重 | 应用内置的通用微任务 |

#### 3.4.2 用户任务管理
- 设置页提供任务清单编辑功能
- 支持增删改查
- 每条任务包含：内容、标签（分类）
- 最少需保留 1 条任务（防止空池）

#### 3.4.3 任务标签分类体系
| 标签 | 说明 | 示例 |
|------|------|------|
| 身体 | 微运动、放松 | 俯卧撑、拉伸、喝水 |
| 学习 | 知识获取 | 背单词、阅读一页书 |
| 整理 | 环境清理 | 整理桌面、清理邮箱 |
| 创意 | 思维发散 | 画草图、写 50 字随笔 |
| 社交 | 人际维护 | 给好友发消息、整理通讯录 |

#### 3.4.4 任务属性扩展
- **预估耗时**：每条任务可标注 1/3/5/10 分钟（默认 3 分钟）
- **完成反馈**：任务完成后用户可标记「轻松 / 适中 / 困难」，用于后续推荐优化
- **冷却时间**：用户近期已完成过的任务，24 小时内降低抽取权重

#### 3.4.5 系统默认任务池（示例）
- 闭眼深呼吸 10 次（身体，1 分钟）
- 喝一大杯水（身体，1 分钟）
- 做 5 个俯卧撑（身体，3 分钟）
- 写下接下来要做的一件事（整理，1 分钟）
- 整理桌面 3 分钟（整理，3 分钟）
- 背 5 个单词（学习，5 分钟）
- 远眺窗外 20 秒（身体，1 分钟）

### 3.5 积分与统计系统

#### 3.5.1 积分规则（MVP 简化版）
- 仅通过**骰子奖励**获得积分
- 完成任务**不直接奖励积分**（避免用户为了刷分而故意选惩罚）
- 积分仅用于展示和长期成就感，MVP 阶段暂不开放兑换

#### 3.5.2 统计数据
- 总积分
- 总掷骰次数
- 完成任务次数
- 直接关闭次数（"逃逸"统计，无惩罚，仅用于自我认知）
- 当前连续完成任务连击数
- **连续完成天数**：自然日维度，每天至少完成 1 个任务即计入

#### 3.5.3 可视化图表（设置页统计面板）
- **周/月趋势图**：折线图展示每日完成任务数、积分获取趋势
- **任务分类饼图**：按标签统计各类型任务完成占比
- **时段热力图**：展示一天中哪些时段最容易触发打断（帮助用户认知习惯）
- **骰子偏好**：展示用户使用各类型骰子的次数占比

### 3.6 历史记录
- 记录最近 100 次打断事件
- 每条记录包含：时间戳、骰子类型、掷出点数、结果（奖励/惩罚）、任务内容、获得积分
- 本地存储，支持历史查看
- **导出功能**：支持导出 CSV/Excel，方便用户外部分析
- **筛选功能**：按日期范围、骰子类型、任务标签筛选记录

---

## 4. 用户旅程 (User Journey)

### 4.1 主流程

```
[用户右键托盘图标 → 开启切点模式]
           ↓
[弹出设置窗口：设定 T1/T2/默认骰子/任务清单]
           ↓
[设置完成 → 窗口隐藏 → 后台静默计时 T1]
           ↓
⏰ [T1 时间到]
           ↓
[屏幕右上角弹出置顶打断窗口]
           ↓
├─→ 路径 A（心流工作）：等待 5-10 秒 → 点击「继续工作」→ 窗口关闭 → 进入下一个 T1
│
├─→ 路径 B（娱乐状态）：被骰子动画吸引 → 点击「掷骰子」
│         ↓
│   ├─→ 奖励（如掷出 6）：显示 +6 分 → 点击「收下」→ 窗口关闭 → 进入下一个 T1
│   │
│   └─→ 惩罚（如掷出 3）：显示随机任务 → 点击「开始任务」→ 窗口关闭
│             ↓
│         [进入 T2 自由时间]
│             ↓
│         [T2 结束]
│             ↓
│         [自动进入下一个 T1]
│
└─→ 路径 C（强制等待中）：倒计时未结束，按钮禁用，必须等待
```

### 4.2 关键设计原则
- **不判断用户当前行为**：无论在工作还是娱乐，均按固定周期触发
- **无关闭惩罚**：直接关闭不扣积分、不弹警告，降低心理负担
- **娱乐化缓冲**：用赌博机制替代说教，让用户主动参与打断过程

### 4.3 全屏/游戏场景处理
- **检测机制**：通过 Windows API（`GetForegroundWindow` + `GetWindowRect`）判断当前是否有应用处于全屏模式
- **处理策略**：
  - 若检测到全屏应用（游戏、视频播放器、PPT 演示）：弹窗延迟到全屏退出后 30 秒内触发
  - 若全屏持续超过 T1 的 1.5 倍时长，强制在副屏显示（如有）或通过声音提醒
  - 用户可在设置中关闭此检测（默认开启）

---

## 5. 界面需求 (UI Requirements)

### 5.1 设置窗口（平时隐藏）
- 工作时长调节（滑块/数字输入）
- 休息时长调节
- 强制等待时长调节
- 默认骰子选择（下拉菜单）
- 用户任务清单管理（增删改查列表）
- 开启/关闭切点模式按钮
- 运行状态指示器

### 5.2 打断弹窗
- 顶部：「⏰ 切点时间到！」标题
- 中部：骰子动画展示区（占 50% 高度）
- 下部：
  - 倒计时进度条
  - 「继续工作」按钮（禁用/激活状态切换）
  - 「掷骰子」按钮（始终可用）
- 整体风格：轻量化、游戏感、不严肃

**UI 设计系统**：
- 主色调：`#6366F1`（靛紫，代表「切点」的切换意象）
- 背景：`#FFFFFF`（白天）/ `#1E1E2E`（夜间跟随系统）
- 字体：中文使用「Noto Sans SC」，英文使用「Inter」
- 圆角：16px（大按钮）/ 8px（卡片）
- 阴影：弹窗使用 `0 4px 24px rgba(0,0,0,0.15)` 营造悬浮感

### 5.3 结果展示（弹窗内切换）
- **奖励结果页**：大字号显示积分，动画反馈
- **惩罚结果页**：任务内容展示，标签分类，操作按钮

---

## 6. 非功能需求

### 6.1 性能
- 后台计时器资源占用极低（< 10MB 内存）
- 弹窗启动延迟 < 500ms
- 动画帧率稳定 60fps

### 6.2 可靠性
- **应用崩溃/重启后**：
  - 优先恢复：剩余工作时长、当前是否处于 T2 休息期
  - 降级策略：若无法精确恢复，至少保留配置数据，重新从 T1 开始计时
  - 数据持久化：每次状态变更（开始 T1、进入 T2、弹窗触发）立即写入 Hive
- **系统休眠/唤醒后**：
  - 休眠前记录时间戳，唤醒后计算实际经过时间
  - 若休眠期间跨越了 T1 结束点，唤醒后立即补弹打断窗口（不跳过）
  - 若处于 T2 期间休眠，唤醒后继续剩余 T2 时长（或至少 1 分钟缓冲）
- **系统锁定/解锁**：
  - 锁屏期间计时器暂停，解锁后继续
  - 若锁屏期间错过打断点，解锁后立即补弹
- **多显示器热插拔**：
  - 主显示器切换时，弹窗自动迁移到新的主显示器
  - 避免弹窗显示在已断开的显示器上导致用户无法交互

### 6.3 本地运行
- MVP 阶段**完全不联网**
- 所有数据本地存储
- 无账号系统、无云端同步

### 6.4 扩展性
- 架构预留 AI 接入接口（见第 8 章）
- UI 架构支持后续 Android 复用

### 6.5 数据安全与隐私
- **本地存储加密**：
  - Hive 数据库使用 AES-256 加密密钥（密钥派生自设备指纹，不上传）
  - 敏感字段（历史记录、统计）加密存储，配置数据可明文
- **崩溃日志**：
  - 本地保存最近 30 天崩溃日志（`logs/crash/` 目录）
  - 日志不包含用户任务内容等隐私数据
  - 提供「导出日志」按钮供用户手动反馈问题
- **数据备份**：
  - 自动备份：每天首次启动时导出 JSON 到 `backups/` 目录，保留最近 7 份
  - 手动备份：设置页提供「立即备份」和「恢复备份」按钮

---

## 7. 数据需求

### 7.1 数据模型

#### Settings（设置）
```json
{
  "work_duration_minutes": 30,
  "rest_duration_minutes": 5,
  "force_wait_seconds": 8,
  "default_dice": "d6"
}
```

#### Task（任务）
```json
{
  "id": "uuid",
  "content": "背5个单词",
  "tag": "英语",
  "is_user_created": true
}
```

#### RollHistory（历史记录）
```json
{
  "timestamp": "2026-04-25T14:30:00",
  "dice_type": "d6",
  "rolled_value": 6,
  "is_reward": true,
  "points_earned": 6,
  "task_content": null
}
```

#### Stats（统计）
```json
{
  "total_points": 42,
  "total_rolls": 15,
  "total_tasks_accepted": 8,
  "total_skips": 7,
  "current_streak": 3
}
```

### 7.2 存储方案
- **技术**: Hive（Flutter 高性能本地数据库）
- **格式**: 二进制键值存储，比 JSON 文件读写更快
- **备份**: 自动导出 JSON 到应用目录，支持手动备份

---

## 8. 技术架构

### 8.1 技术栈
| 层级 | 技术 | 说明 |
|------|------|------|
| 框架 | **Flutter** | 跨平台，一套代码覆盖 Windows + Android |
| 语言 | Dart | 类似 Java/C++，学习成本 1-2 周 |
| 状态管理 | flutter_riverpod | 现代、简洁、可测试 |
| 本地存储 | Hive | 高性能 NoSQL，支持复杂对象 |
| 窗口管理 | window_manager | 置顶、尺寸控制、位置控制 |
| 系统托盘 | system_tray | 托盘图标与菜单 |
| 3D 渲染 | flutter_cube / flutter_gl | 3D 骰子模型渲染、动画 |
| 全局快捷键 | hotkey_manager | 系统级快捷键监听 |
| 日志 | logger + path_provider | 本地日志记录与文件管理 |
| 网络层 | Dio | 预留，MVP 不调用 |
| 序列化 | freezed + json_annotation | 自动生成模型代码 |

### 8.2 架构分层

```
┌─────────────────────────────────────┐
│  Presentation (UI 层)                │
│  - 设置页 / 打断弹窗 / 动画组件       │
│  - 状态管理: Riverpod                 │
├─────────────────────────────────────┤
│  Domain (业务层)                     │
│  - TimerService: 工作/休息倒计时      │
│  - DiceService: 多面骰概率计算        │
│  - TaskService: 任务池随机抽取        │
│  - PointService: 积分与统计           │
│  - 纯 Dart 逻辑，零平台依赖           │
├─────────────────────────────────────┤
│  Data (数据层)                       │
│  ┌──────────────┐  ┌─────────────┐  │
│  │ LocalStorage │  │ AIRepository│  │
│  │ (Hive)       │  │ Interface   │  │
│  └──────────────┘  └──────┬──────┘  │
│                           │         │
│                    现在: MockAIRepo   │
│                    未来: HttpAIRepo   │
└─────────────────────────────────────┘
```

### 8.3 关键设计决策
- **Repository 模式**: AI 层通过接口注入，现在用 Mock 实现，未来替换为云端实现时**零业务代码改动**
- **服务层纯 Dart**: 所有业务逻辑与 Flutter UI 解耦，便于单元测试

---

## 9. AI 扩展规划（Phase 2）

### 9.1 功能规划
| 功能 | 描述 | 实现方式 |
|------|------|----------|
| 智能任务拆解 | 用户输入大目标（如"准备考研"），AI 拆解为微任务 | 调用云端大模型 API |
| 对话式鼓励 | 用户可与 AI 对话，获得任务建议或心理支持 | 调用云端大模型 API |
| 任务推荐优化 | 基于用户历史完成数据，推荐更合适的任务 | 云端分析 + 本地缓存 |

### 9.2 架构预留
```dart
abstract class AIRepository {
  Future<List<Task>> breakDownGoal(String goal);
  Future<String> chat(String message, {List<Map<String, String>>? history});
}

// MVP: Mock 实现（本地规则匹配，不联网）
class MockAIRepository implements AIRepository { ... }

// Phase 2: 云端实现（替换即可）
class CloudAIRepository implements AIRepository { ... }
```

### 9.3 数据隐私
- MVP 阶段所有数据本地存储，不上传
- Phase 2 接入云端时，仅传输必要的文本数据（目标描述、对话内容）
- 敏感数据（历史记录、统计）仍保留本地

---

## 10. 用户引导与首次体验

### 10.1 首次启动引导
- **步骤 1（欢迎页）**：产品理念介绍，展示核心循环动画
- **步骤 2（配置引导）**：推荐新手配置（T1=25 分钟，T2=5 分钟，d6），用户可一键采用或自定义
- **步骤 3（添加任务）**：引导用户添加 3-5 条个人任务，提供「快速导入」模板
- **步骤 4（快捷键演示）**：演示托盘菜单操作和全局快捷键
- **步骤 5（试玩）**：触发一次模拟打断弹窗，让用户熟悉交互流程（不计入真实计时）

### 10.2 日常使用提示
- 第 1-3 天：每次弹窗显示「小贴士」（如快捷键提示、功能说明）
- 第 7 天：成就提示（"你已使用切点 7 天"）
- 连续 3 次关闭弹窗：温和提示「试试掷骰子？可能会有惊喜」

## 11. 开发路线图

### Phase 1: MVP（5-6 周）

| 周次 | 模块 | 交付物 |
|------|------|--------|
| **W1** | 环境搭建 + 项目骨架 | 可运行的 Flutter Windows 应用，空窗口正常显示 |
| **W2** | 系统托盘 + 窗口管理 | 应用可隐藏到托盘，点击托盘弹出设置窗口，置顶弹窗正常显示 |
| **W3** | 核心计时 + 打断循环 | 完整的 T1→弹窗→关闭/T2→T1 循环，强制等待逻辑 |
| **W4** | 游戏化系统 | 多面骰逻辑、任务抽取、积分计算、历史记录存储 |
| **W5** | 动画 + UI 美化 | 骰子旋转动画、结果展示页、整体视觉统一 |
| **W6** | Polish + 打包 | 边界情况处理、Windows 安装包 (`flutter build windows`)、自测修复 |

### Phase 2: 扩展（待定）
- Android 移植（复用 90% 代码）
- 云端 AI 接入
- 积分兑换系统（如解锁新骰子皮肤）
- Roguelike 局外成长机制（大富翁/地牢探险）

## 12. 运营与增长

### 12.1 分发策略
- **Windows 安装包**：GitHub Releases 发布 `.exe` 安装包
- **免安装版**：提供便携版 `.zip`，解压即用
- **应用商店**：后续上架 Microsoft Store

### 12.2 用户反馈
- **内置反馈**：设置页提供「发送反馈」按钮，收集建议与 Bug 报告
- **使用统计（可选）**：匿名统计功能使用频率（用户可关闭），用于产品优化
- **社区建设**：GitHub Discussions 作为用户交流社区

### 12.3 更新机制
- **自动检查更新**：启动时检查 GitHub Releases 最新版本
- **更新提示**：非强制，用户可选择跳过
- **更新日志**：展示最近 3 个版本的变更说明

---

## 13. 风险与假设

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| 用户直接关闭弹窗，完全不用骰子功能 | 产品沦为普通定时提醒 | 优化动画吸引力，后续加入「连续关闭惩罚」（如缩短下次间隔） |
| 心流用户厌烦定时弹窗 | 目标用户流失 | 提供「勿扰模式」或更长的 T1 上限（如 90 分钟） |
| 任务池内容快速枯竭 | 用户重复做同样任务感到无聊 | 系统任务库持续更新，AI 接入后动态生成 |
| Windows 桌面端市场有限 | 用户量不足 | 快速推进 Android 版本，覆盖主要使用场景 |

---

## 14. 术语表

| 术语 | 定义 |
|------|------|
| T1 | 工作时长（Work Duration），两次打断之间的间隔 |
| T2 | 休息时长（Rest Duration），接受惩罚任务后的自由时间 |
| 强制等待 | 弹窗出现后，「继续工作」按钮的禁用时间段 |
| 逃逸 | 用户在强制等待结束后直接关闭弹窗，不做任何操作 |
| 惩罚 | 掷骰子未中奖励时，需要完成一个随机任务 |
| 奖励 | 掷骰子中最大点数时，获得积分，无任务 |
| MockAI | MVP 阶段模拟 AI 功能的本地规则引擎 |

---

*文档结束。本 PRD 为开发唯一依据，后续功能变更需更新本文档。*
